<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.redoy.aistasker">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.AirtaskerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.WithActionBar">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.IntroductionActivity" />
        <activity
            android:name=".activities.SignUpActivity"
            android:label="Sign Up" />
        <activity android:name=".activities.MenuActivity" />
        <activity
            android:name=".activities.LogInActivity"
            android:label="Log In" />
        <activity android:name=".activities.PostTaskActivity" />
        <activity android:name=".activities.PrivateConversationActivity" />
        <activity
            android:name=".activities.ReferralsActivity"
            android:label="Get Airtasker credit" />
        <activity android:name=".activities.HelpActivity" />
        <activity
            android:name=".activities.SettingsActivity"
            android:label="Settings" />
        <activity
            android:name=".activities.TaskAlertsActivity"
            android:label="Task alerts" />
        <activity
            android:name=".activities.NotificationsActivity"
            android:label="Notifications" />
        <activity
            android:name=".activities.ReviewListActivity"
            android:label="Review" />
        <activity
            android:name=".activities.PaymentSettingsActivity"
            android:label="Payment Settings" />
        <activity android:name=".activities.PaymentHistoryActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.DashboardActivity"></activity>
    </application>

</manifest>